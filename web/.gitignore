node_modules
dist
.env
.env.local
.env.*.local
*.log

# Full city photo libraries stay on R2/CDN. Do not commit those trees.
# public/bundled/ is the OSS fallback (Chongqing) and should stay tracked.
public/city/
public/city-opt/

# IDE / 本地杂物（不参与运行）
.idea/
.tmp-*
docs/city/
.claude/
.claude-tmp/
vite.local-sse.config.ts

# 本地生成的代码导航快照（可重建，不能覆盖权威契约）
docs/codebase/
